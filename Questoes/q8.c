#include <stdio.h>

int main() {

    int valor, aoquadrado;

    puts("Digite um valor inteiro: ");
    scanf("%d", &valor);

    aoquadrado = valor * valor;

    printf("O número digitado ao quadrado é: %d", aoquadrado);

}